{
  "name": "ogawaya",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:firestore-rules": "firebase --config firebase/firebase.json emulators:exec --only firestore --project ogawaya-rules-behavior-test \"node --test tests/firebase/firestoreRulesBehavior.test.mjs\""
  },
  "dependencies": {
    "firebase-admin": "^13.9.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.14.0",
    "firebase-tools": "^15.19.1"
  }
}
